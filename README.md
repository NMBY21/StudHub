# StudyHub MERN Project

## Setup
1. cd backend && npm install && npm start
2. cd frontend && npm install && npm start
studyhub/
  backend/
    models/
    routes/
    controllers/
    server.js
  frontend/
    src/
      components/
      pages/
      context/
      App.js
