import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

import './index.css'; // optional, create for basic styles

createRoot(document.getElementById('root')).render(<App />);
